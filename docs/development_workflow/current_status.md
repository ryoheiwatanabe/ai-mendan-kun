# 現在の開発・検証状況

更新日：2026-09-22。機能文書中の日付付き検証は、その時点の記録として扱います。

## ソースと配信

| 対象 | 確認した状態 |
| --- | --- |
| GitHub main | #67を含む#66を取り込み、#65で統合。統合commitは`94061d8` |
| ローカル | 上記mainへ更新。この文書更新も含む最新mainを次の作業開始点とする |
| 比較元 | レビュー対象`29d1b5c`と#67取り込み後`b306b94`はGitのtreeが一致。統合時の追加コード差分はブラウザテスト2ファイルのみ |
| 公開環境 | 9月21日の統合版配信記録と、Cloudflare APIが返す現行version IDの一致を確認。今回の同期ではデプロイしていない |
| 公開画面の確認 | 今回の確認環境ではCSS取得がCloudflare 403（1010）となり、表示確認は未完了。配信障害か接続環境の制限かは未確定 |

mainの更新と本番デプロイは別の工程です。配信記録とversion IDの一致は、配信物とGitの全ファイルを比較した結果ではありません。

旧比較実験の文書PR #63・#64はOPENのままで、この統合には含めていません。

## 統合した機能

- 文字・音声の会話状態、理解した質問の確認、回答停止と復帰。
- JEVによる回答範囲の選別、短い候補の生成、6軸の点検、回数と時間を制限した修復。
- `/admin`での採点・音声設定、設定の版管理、本文を含まない処理計測。
- `/admin/intake`での資料取り込み・確認・承認。重複操作や並行更新の制御。
- 非表示指定、承認・現行版・公開範囲の再確認、管理APIの認証。
- 初期状態ではオフの「開発者モード」。応答速度や回答ごとの処理情報を会話欄の外で確認可能。

JEV版の生成ProviderはOpenCode Go / OpenAIです。共有設定テンプレートは`legacy`のままで、導入環境ごとに回答経路とProviderを選びます。実環境の設定、本人資料、Secret値はGitへ含めません。

## 今回の検証

| 確認 | 結果 |
| --- | --- |
| 型チェック・コア | `npm run check`：506件通過。テスト修正後の型チェックも通過 |
| ブラウザ | 全100件を実行して94件通過・6件失敗。古いボタン名、再生速度設定、質問確定イベントをfixtureへ反映。該当分の再実行で100件すべての成功を確認 |
| 最終fixture | 別AIの指摘で音声のイベント順を実APIへ合わせ、該当1件も再通過。確認条件は維持 |
| Workerビルド | `npm run build:worker`通過。その後はテスト・文書のみ変更 |
| 操作レビュー | ChromeのComputer UseでPCと狭い画面を操作。回答・音声設定を架空データへ置換し、会話・下書き・読み返し等の問題を6件再現 |
| 独立点検 | 別AIが統合差分、以前の指摘への対応、認証、Git除外対象、追加テスト差分を確認 |

修正後の全ブラウザテストを一括では再実行していません。外部AIへの新たな実API評価、実マイクでの会話、実機スマホのキーボード、人による受け入れは今回の確認に含めません。GitHub Actionsは追加していません。

## 次の面接官UX改善

全6件とも未対応です。操作レビューはAIによる再現確認で、参加者を募ったユーザーテストではありません。

| 順序 | 問題 | 完了を確認する操作 |
| --- | --- | --- |
| 1 | 説明画面への往復で会話・下書きを失う | 説明を開閉しても会話・入力・読んでいた位置が残る |
| 1 | 回答待ちに書いた次の質問が失敗処理や候補送信で消える | 失敗・停止・完了・候補選択を試して下書きを保持する |
| 2 | 狭い画面で質問候補が会話領域を圧迫する | 全18候補と長い履歴を使い、入力・読み返しの配置を確認する |
| 2 | 音声画面の入力欄が履歴の上にあり、往復が増える | 長い履歴の回答を読んだ位置から次の質問へ移れる |
| 2 | 音声履歴を読み返している途中に末尾へ移動する | 新しい応答が来ても読み返し位置を維持する |
| 3 | マイクを使わない手入力でも「マイク使用中」と表示する | 手入力・収音・停止の状態に表示が一致する |

説明文の追加より、入力保持・配置・スクロール・状態表示の変更を優先します。[作業プラン](../ux_review/work_plan_2026-09-22.md)の後にゴールを設定し、改善実装へ進みます。回答品質、実音声、長時間利用の本人確認は引き続き必要です。
